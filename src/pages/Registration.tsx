import React from 'react';
import TelegramForm from '../components/TelegramForm.tsx';

const App: React.FC = () => {
    return (
        <div className={'regContent'}>
            <TelegramForm />
        </div>
    );
};

export default App;